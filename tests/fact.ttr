# a simple factorial function
def fact(x int) int:
    # if x is zero, then 1
    if x == 0:
        return 1
    else:
        return x * fact(x - 1)

def main( ):
    background:
        print("Enter n: ")
        n = read_int( )
        # n = 5
        print(n, "! = ", fact(n))
    print("This part is done!")

